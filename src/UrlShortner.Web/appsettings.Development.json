// src/UrlShortner.Web/appsettings.Development.json
{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "UrlShortner": "Debug"
    }
  },

  "EmailSettings": {
    "SmtpHost": "smtp.ethereal.email",
    "SmtpPort": 587
    // Username and password will be set via user secrets or environment variables
  }
}